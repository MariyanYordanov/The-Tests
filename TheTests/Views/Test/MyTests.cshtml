@model IEnumerable<TestViewModel>

@{
    ViewData["Title"] = "My Tests";
}

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<div class="container mt-4">
    <h1 class="mb-4">My Tests</h1>

    @if (!Model.Any())
    {
        <div class="alert alert-info">
            You have not created any tests yet. <a href="@Url.Action("CreateTest", "Test")">Create a new test</a>.
        </div>

    }
    else
    {
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Category</th>
                    <th>Status</th>
                    <th>Actions</th>
                    <th>Statistics</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var test in Model.OrderByDescending(x => x.Id))
                {
                    <tr>
                        <td>
                            <form asp-controller="Test" asp-action="EditTest" method="get">
                                <input type="hidden" name="testId" value="@test.Id" />
                                <button type="submit" class="btn btn-link">@test.Title</button>
                            </form>
                        </td>
                        <td>@test.CategoryName</td>
                        <td>
                            @if ((bool)test.IsActive)
                            {
                                <span class="badge bg-success">Active</span>
                            }
                            else
                            {
                                <span class="badge bg-warning">Inactive</span>
                            }
                        </td>
                        <td>
                            @if (!(bool)test.IsActive)
                            {
                                <form asp-controller="Test" asp-action="ActivateTest" method="post" class="d-inline">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="testId" value="@test.Id" />
                                    <button type="submit" class="btn btn-sm btn-primary">Activate</button>
                                </form>
                            }

                            <form asp-controller="Test" asp-action="SolveTest" method="post" class="d-inline">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="testId" value="@test.Id" />
                                <button type="submit" class="btn btn-sm btn-danger">Solve</button>
                            </form>

                            <form asp-controller="Test" asp-action="DeleteTest" method="post" class="d-inline">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="testId" value="@test.Id" />
                                <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                            </form>

                            <form asp-controller="Test" asp-action="PublishTest" method="post" class="d-inline">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="testId" value="@test.Id" />
                                <button type="submit" class="btn btn-sm btn-secondary">Publish</button>
                            </form>
                        </td>
                        <td>
                            <form asp-controller="Test" asp-action="Statistics" method="get">
                                <input type="hidden" name="testId" value="@test.Id" />
                                <button type="submit" class="btn btn-sm btn-info">View Statistics</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>